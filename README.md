# Filesystem-Crawler
Data project leading to the build of a filesystem crawler that creates a tree of files and folders, along with a crawler capable of distributing the files to workers efficiently.
